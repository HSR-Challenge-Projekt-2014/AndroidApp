<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ch.hsr.challp.museum" >

    <!-- Needed in order to allow the app to enable Bluetooth -->
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:launchMode="singleInstance"
        android:theme="@style/AppTheme" >
        <activity
            android:name="ch.hsr.challp.museum.HomeActivity"
            android:label="@string/title_activity_home" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="ch.hsr.challp.museum.MonitoringActivity"
            android:label="@string/title_activity_monitoring" >
        </activity>
        <activity
            android:name="ch.hsr.challp.museum.BeaconTest"
            android:label="@string/title_activity_beacon_test" >
        </activity>
        <service
            android:name=".BeaconScanService"
            android:exported="false" >
        </service>
    </application>

</manifest>
